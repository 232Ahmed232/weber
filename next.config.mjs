/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'demo.awaikenthemes.com','scontent.fisb5-2.fna.fbcdn.net']
    }
};

export default nextConfig;
